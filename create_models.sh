npx sequelize-cli model:generate --name User --attributes name:string
npx sequelize-cli model:generate --name Artist --attributes order:number,name:string,song:string,country:string
npx sequelize-cli model:generate --name Vote --attributes points:number
